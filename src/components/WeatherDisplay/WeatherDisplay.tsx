import type { WeatherCardModel } from "../../models/weather.car.model";
import "./WeatherDisplay.css"

function WeatherDisplay(weatherData: WeatherCardModel){
  console.log(weatherData)
  return (
    <div>

    </div>
  )
}

export default WeatherDisplay;